// Future database migrations are added here as new version blocks
// Example:
// db.version(2).stores({ habits: 'id, archived, active, category, timeBlock, sortOrder, notificationTime' })
//   .upgrade(tx => tx.table('habits').toCollection().modify(h => { h.notificationTime = null }))

export {}
